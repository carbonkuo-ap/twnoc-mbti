# MBTI 測驗應用程式 - 環境變數範例
# 複製此檔案為 .env.local 用於本地開發

# =================================
# 加密密鑰設定
# =================================
# 用於加密測驗結果和敏感資料的主密鑰
# 生產環境中請使用 64 字元的隨機字串
NEXT_PUBLIC_ENCRYPTION_KEY=your-64-character-random-encryption-key-here-change-this

# =================================
# 管理員認證設定
# =================================
# 管理員密碼的雜湊值（使用 PBKDF2）
# 使用 lib/security.ts 中的 hashPassword 函數生成
NEXT_PUBLIC_ADMIN_HASH=your-admin-password-hash-here

# 管理員密碼雜湊的 Salt 值
# 使用隨機 32 字元字串
NEXT_PUBLIC_ADMIN_SALT=your-32-character-random-salt-here

# =================================
# 生產環境設定說明
# =================================
# 在 GitHub Actions 或其他 CI/CD 中設定以下 Secrets：
# - NEXT_PUBLIC_ENCRYPTION_KEY: 64 位隨機加密密鑰
# - NEXT_PUBLIC_ADMIN_HASH: 管理員密碼的 PBKDF2 雜湊
# - NEXT_PUBLIC_ADMIN_SALT: 雜湊用的隨機 Salt

# =================================
# 密鑰生成指南
# =================================
# 1. 加密密鑰生成（64 字元）：
#    node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# 2. Salt 生成（32 字元）：
#    node -e "console.log(require('crypto').randomBytes(16).toString('hex'))"
#
# 3. 管理員密碼雜湊生成：
#    使用 lib/security.ts 中的 hashPassword('您的密碼', '您的Salt', 10000)

# =================================
# 安全注意事項
# =================================
# ⚠️  絕對不要將真實的密鑰或雜湊值提交到版本控制系統
# ⚠️  生產環境必須使用強隨機密鑰
# ⚠️  定期更換加密密鑰和管理員密碼
# ⚠️  確保環境變數在部署平台中正確設定

# =================================
# GitHub Pages 部署設定
# =================================
# 部署到 GitHub Pages 時的基本路徑
NEXT_PUBLIC_BASE_PATH=/your-repository-name

# 管理員使用者名稱
NEXT_PUBLIC_ADMIN_USERNAME=your-admin-username

# =================================
# Firebase 配置 (跨設備資料同步)
# =================================
# 從 Firebase Console 獲取以下配置資訊
# 1. 前往 https://console.firebase.google.com/
# 2. 建立新專案或選擇現有專案
# 3. 在專案設定中找到 Web 應用程式配置

NEXT_PUBLIC_FIREBASE_API_KEY=your-firebase-api-key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_DATABASE_URL=https://your-project-id-default-rtdb.firebaseio.com/
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your-messaging-sender-id
NEXT_PUBLIC_FIREBASE_APP_ID=your-app-id

# =================================
# Firebase 設定說明
# =================================
# 1. 建立 Firebase 專案
# 2. 啟用 Realtime Database
# 3. 設定資料庫規則（建議開發時使用測試模式）
# 4. 複製 Web 應用程式配置到上方環境變數

# Firebase 資料庫安全規則範例：
# {
#   "rules": {
#     ".read": false,
#     ".write": false,
#     "testResults": {
#       ".read": true,
#       ".write": true
#     },
#     "otpUsage": {
#       ".read": true,
#       ".write": true
#     }
#   }
# }